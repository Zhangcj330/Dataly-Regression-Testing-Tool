# Dataly-Regression-Testing-Tool

Clone the repository: 
git clone [Url of the repository]
cd [repository]

Install dependencies: 
pip install -r requirements.txt


Run App: 
streamlit run app.py --server.maxUploadSize=1028
